/*Ejercicio 9:Ordenar una matriz de cadenas alfabéticamente

Escriba una función que tome una matriz de cadenas como argumento. Ordene los elementos de la matriz alfabéticamente. Devuelve el resultado.
*/

const myFunction = (arr) => {
    return arr.sort();
  }

console.log(myFunction(['z', 'c', 'd', 'a', 'y', 'a', 'w']))
console.log(console.log(myFunction(['z', 'c', 'd', 'a', 'y', 'a', 'w'])))

/*Interpretación: use el método .sort que me permite oredenar los elementos del array y devolver una nuevo*/